interface:
  display_name: "Video Summary"
  short_description: "Turn raw travel clips into one final edited video."
  default_prompt: "Use $video-summary to turn raw travel clips plus a natural-language editing prompt into one finished video."

policy:
  allow_implicit_invocation: true
